import React, { useEffect, useMemo, useState } from 'react';
import { Navigate } from 'react-router-dom';
import { CheckCircle, Edit3, FileText, Heart, Image as ImageIcon, Plus, Trash2, Users, XCircle } from 'lucide-react';
import { useAuth } from '../context/AuthContext';
import { mockActivityFeed, mockDonations, mockNews, mockUsers } from '../data/mockData';
import { db } from '../firebase';
import { addDoc, collection, deleteDoc, doc, onSnapshot, setDoc } from 'firebase/firestore';

type Tab = 'members' | 'news' | 'activity' | 'donations';
const seedData: Record<Tab, any[]> = { members: mockUsers, news: mockNews, activity: mockActivityFeed, donations: mockDonations };

export default function Dashboard() {
  const { user } = useAuth();
  const [tab, setTab] = useState<Tab>('members');
  const [records, setRecords] = useState<Record<Tab, any[]>>({ members: [], news: [], activity: [], donations: [] });
  const [editing, setEditing] = useState<any>(null);
  const [draft, setDraft] = useState<any>({});
  const [message, setMessage] = useState('');

  useEffect(() => {
    const unsubscribers = (Object.keys(seedData) as Tab[]).map((key) => onSnapshot(collection(db, key), async (snapshot) => {
      if (snapshot.empty) {
        setRecords((current) => ({ ...current, [key]: seedData[key] }));
        return;
      }
      setRecords((current) => ({ ...current, [key]: snapshot.docs.map((item) => ({ id: item.id, ...item.data() })) }));
    }, () => setRecords((current) => ({ ...current, [key]: seedData[key] }))));
    return () => unsubscribers.forEach((unsubscribe) => unsubscribe());
  }, []);

  if (!user || (user.role !== 'admin' && user.role !== 'superadmin')) return <Navigate to="/" replace />;
  const config = { members: { label: 'Members', icon: Users, title: 'Member approvals', add: 'Add member' }, news: { label: 'News', icon: FileText, title: 'News & announcements', add: 'Add news' }, activity: { label: 'Activity', icon: ImageIcon, title: 'Community activity', add: 'Add activity' }, donations: { label: 'Donations', icon: Heart, title: 'Donation ledger', add: 'Add donation' } }[tab];
  const list = records[tab];
  const pending = useMemo(() => records.members.filter((member) => member.status === 'pending'), [records.members]);
  const fields: Record<Tab, string[]> = { members: ['name', 'fatherName', 'cnic', 'phone', 'address', 'bloodGroup', 'status', 'role'], news: ['title', 'category', 'date', 'description'], activity: ['imageUrl', 'description', 'date'], donations: ['donorName', 'amount', 'purpose', 'date'] };
  const openAdd = () => { setEditing({ id: null }); setDraft(tab === 'members' ? { name: '', fatherName: '', cnic: '', phone: '', address: '', bloodGroup: 'O+', status: 'approved', role: 'member' } : tab === 'news' ? { title: '', category: 'Announcement', date: new Date().toISOString().slice(0, 10), description: '', comments: [] } : tab === 'activity' ? { imageUrl: '', description: '', date: new Date().toISOString().slice(0, 10) } : { donorName: '', amount: 0, purpose: '', date: new Date().toISOString().slice(0, 10) }); };
  const openEdit = (item: any) => { setEditing({ id: item.id }); setDraft({ ...item }); };
  const save = async (event: React.FormEvent) => { event.preventDefault(); const payload = { ...draft }; delete payload.id; try { if (editing.id) await setDoc(doc(db, tab, editing.id), payload); else await addDoc(collection(db, tab), payload); setEditing(null); setMessage('Saved to Firebase Firestore.'); } catch { setMessage('Firebase save failed. Check Firestore rules.'); } };
  const remove = async (id: string) => { if (!window.confirm('Delete this item permanently?')) return; try { await deleteDoc(doc(db, tab, id)); setMessage('Deleted from Firebase Firestore.'); } catch { setMessage('Firebase delete failed. Check Firestore rules.'); } };
  const approve = async (id: string, status: 'approved' | 'rejected') => { try { await setDoc(doc(db, 'members', id), { status }, { merge: true }); setMessage(`Member ${status}.`); } catch { setMessage('Firebase update failed.'); } };

  return <div className="min-h-screen bg-[#f5f7f4] px-4 py-8 text-gray-900 sm:px-6 lg:px-8"><div className="mx-auto max-w-7xl"><div className="mb-8 flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><p className="text-xs font-bold uppercase tracking-[.18em] text-amber-600">Zwanan Jawkhela · Firebase</p><h1 className="mt-2 font-serif text-4xl font-medium text-primary-950">Admin workspace</h1><p className="mt-1 text-sm text-gray-500">Connected to Firestore project: <strong>{db.app.options.projectId}</strong></p></div><div className="rounded-xl bg-primary-950 px-4 py-3 text-right text-white"><p className="text-xs text-white/60">Signed in as</p><p className="font-bold">{user.name}</p></div></div><div className="grid gap-6 lg:grid-cols-[230px_1fr]"><aside className="h-fit rounded-2xl border border-gray-200 bg-white p-2 shadow-sm">{(Object.keys(config) as Tab[]).map((key) => { const item = config[key]; const Icon = item.icon; return <button key={key} onClick={() => { setTab(key); setEditing(null); }} className={`flex w-full items-center gap-3 rounded-xl px-4 py-3 text-left text-sm font-semibold transition ${tab === key ? 'bg-primary-950 text-white' : 'text-gray-600 hover:bg-gray-50'}`}><Icon size={17} />{item.label}</button>; })}</aside><section><div className="mb-4 flex items-center justify-between"><div><h2 className="font-serif text-3xl font-medium">{config.title}</h2>{tab === 'members' && <p className="mt-1 text-sm text-gray-500">{pending.length} pending approval{pending.length === 1 ? '' : 's'}</p>}</div><button onClick={openAdd} className="flex items-center gap-2 rounded-xl bg-primary-700 px-4 py-2.5 text-sm font-bold text-white shadow-sm transition hover:bg-primary-600"><Plus size={16} /> {config.add}</button></div>{message && <p className="mb-4 rounded-xl border border-green-200 bg-green-50 px-4 py-3 text-sm text-green-800">{message}</p>}{editing && <form onSubmit={save} className="mb-5 grid gap-3 rounded-2xl border border-amber-200 bg-amber-50 p-5 sm:grid-cols-2">{fields[tab].map((key) => <label key={key} className="text-xs font-bold capitalize text-gray-600">{key}{key === 'role' ? <select value={draft[key] ?? 'member'} onChange={(e) => setDraft({ ...draft, [key]: e.target.value })} className="mt-1 w-full rounded-lg border border-gray-200 bg-white px-3 py-2 text-sm font-normal outline-none focus:border-primary-500"><option value="member">Member</option><option value="admin">Admin</option><option value="superadmin">Superadmin</option></select> : key === 'status' ? <select value={draft[key] ?? 'pending'} onChange={(e) => setDraft({ ...draft, [key]: e.target.value })} className="mt-1 w-full rounded-lg border border-gray-200 bg-white px-3 py-2 text-sm font-normal outline-none focus:border-primary-500"><option value="pending">Pending</option><option value="approved">Approved</option><option value="rejected">Rejected</option></select> : <input required={key !== 'imageUrl'} type={key === 'amount' ? 'number' : key === 'date' ? 'date' : 'text'} value={draft[key] ?? ''} onChange={(e) => setDraft({ ...draft, [key]: key === 'amount' ? Number(e.target.value) : e.target.value })} className="mt-1 w-full rounded-lg border border-gray-200 bg-white px-3 py-2 text-sm font-normal outline-none focus:border-primary-500" />}</label>)}<div className="flex items-end gap-2"><button className="rounded-lg bg-primary-700 px-4 py-2 text-sm font-bold text-white">Save to Firebase</button><button type="button" onClick={() => setEditing(null)} className="rounded-lg border border-gray-300 px-4 py-2 text-sm font-bold">Cancel</button></div></form>}<div className="space-y-3">{list.map((item) => <div key={item.id} className="flex flex-col gap-4 rounded-2xl border border-gray-200 bg-white p-4 shadow-sm sm:flex-row sm:items-center sm:justify-between"><div className="min-w-0"><p className="font-semibold">{item.name || item.title || item.description || item.donorName} {item.role && <span className="ml-2 text-[10px] uppercase font-bold text-gray-500 border border-gray-200 rounded px-1">{item.role}</span>}</p><p className="truncate text-xs text-gray-500">{item.cnic || item.category || item.purpose || item.date || item.id}</p>{tab === 'members' && <span className={`mt-2 inline-flex rounded-full px-2 py-1 text-[10px] font-bold uppercase ${item.status === 'approved' ? 'bg-green-100 text-green-700' : item.status === 'rejected' ? 'bg-red-100 text-red-700' : 'bg-amber-100 text-amber-700'}`}>{item.status}</span>}</div><div className="flex shrink-0 items-center gap-2">{tab === 'members' && item.status === 'pending' && <><button onClick={() => approve(item.id, 'approved')} className="rounded-lg bg-green-50 p-2 text-green-700" title="Approve"><CheckCircle size={17} /></button><button onClick={() => approve(item.id, 'rejected')} className="rounded-lg bg-red-50 p-2 text-red-700" title="Reject"><XCircle size={17} /></button></>}<button onClick={() => openEdit(item)} className="rounded-lg bg-gray-100 p-2 text-gray-600" title="Edit"><Edit3 size={16} /></button><button onClick={() => remove(item.id)} className="rounded-lg bg-red-50 p-2 text-red-600" title="Delete"><Trash2 size={16} /></button></div></div>)}{list.length === 0 && <div className="rounded-2xl border border-dashed border-gray-300 bg-white p-10 text-center text-sm text-gray-500">No records yet.</div>}</div></section></div></div></div>;
}
